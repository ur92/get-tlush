nodeLinker: node-modules

# Pin to the public npm registry so a developer's global JFrog/Artifactory
# config does not leak private mirror URLs into yarn.lock (breaks CI installs).
npmRegistryServer: "https://registry.npmjs.org"

enableGlobalCache: true
